spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/parking-control-db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true